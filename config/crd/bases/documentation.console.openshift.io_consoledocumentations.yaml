
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.4.1
  creationTimestamp: null
  name: consoledocumentations.documentation.console.openshift.io
spec:
  group: documentation.console.openshift.io
  names:
    kind: ConsoleDocumentation
    listKind: ConsoleDocumentationList
    plural: consoledocumentations
    singular: consoledocumentation
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: ConsoleDocumentation is the Schema for the consoledocumentations
          API
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: ConsoleDocumentationSpec defines the desired state of ConsoleDocumentation
            properties:
              appName:
                type: string
              description:
                type: string
              displayName:
                type: string
              durationMinutes:
                type: integer
              featureFlag:
                type: string
              icon:
                type: string
              img:
                type: string
              markDown:
                type: string
              provider:
                type: string
              type:
                type: string
              url:
                type: string
            required:
            - durationMinutes
            type: object
          status:
            description: ConsoleDocumentationStatus defines the observed state of
              ConsoleDocumentation
            properties:
              enabled:
                type: boolean
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
status:
  acceptedNames:
    kind: ""
    plural: ""
  conditions: []
  storedVersions: []
